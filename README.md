# chinmaymodi.github.io

Personal portfolio site. Built with React, TypeScript, and Vite.

## Development

```bash
npm install
npm run dev
```

## Deploy

Push to `master`. GitHub Actions builds and deploys automatically.

To deploy manually: `npm run build` and push `dist/` (not recommended — use CI).
